package com.codeonist.ems.rest.controllers;

/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.security.authentication.AuthenticationManager;
import org.springframework.security.authentication.BadCredentialsException;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RestController;

import com.codeonist.ems.beans.AuthenticationRequest;
import com.codeonist.ems.beans.AuthenticationResponse;
import com.codeonist.ems.config.util.JwtUtil;
import com.codeonist.ems.services.EmsUserDetailService;
*/
//@RestController
public class AuthenticationController {
	/*
	 * @Autowired private AuthenticationManager authenticationManager;
	 * 
	 * @Autowired private JwtUtil jwtTokenUtil;
	 * 
	 * @Autowired private EmsUserDetailService emsUserDetailService;
	 * 
	 * @RequestMapping(value = "/authenticate", method = RequestMethod.POST) public
	 * ResponseEntity<?> createAuthenticationToken(@RequestBody
	 * AuthenticationRequest authenticationRequest) throws Exception {
	 * 
	 * try { authenticationManager.authenticate(new
	 * UsernamePasswordAuthenticationToken( authenticationRequest.getUsername(),
	 * authenticationRequest.getPassword())); } catch (BadCredentialsException e) {
	 * throw new Exception("Incorrect username or password", e); }
	 * 
	 * final UserDetails userDetails =
	 * emsUserDetailService.loadUserByUsername(authenticationRequest.getUsername());
	 * final String jwt = jwtTokenUtil.generateToken(userDetails);
	 * 
	 * return ResponseEntity.ok(new AuthenticationResponse(jwt)); }
	 */
}
